/*program to input 5 numbetrs from the user and print their sum*/
#include<stdio.h>
int main()
{
    int num,i,sum=0;
    for(i=1;i<=5;i++)
    {
        printf("Enter a number : ");
        scanf("\n%d",&num);
        sum+=num;
    }
    printf("Sum is %d ",sum);

}
